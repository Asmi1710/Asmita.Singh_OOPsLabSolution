package com.gl.credentialGeneration;
// imported package
import com.gl.emailapp.UserUI.*;
public class mainClass {

    // this is the main class which executes the entire project by creating object of class UI
    // and calling its function initiation()
    public static void main(String[] args) {
        UserUI ui = new UserUI();
        ui.initiation();
    }
}
